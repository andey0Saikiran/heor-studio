# P1 module blueprints — DRAFT (not yet verification-passed)

These six blueprints were generated by a parallel design workflow on 2026-07-25.
The workflow's adversarial-verification and revision passes were **cut short by a
Fable-5 monthly spend limit**, so treat every file here as an **unverified design
draft**, not a checked specification.

Verification status at time of writing:
- `point_prevalence` — design + partial verify (defects flagged); revise pass did NOT run
- `period_prevalence` — design + vector-verify (defects flagged); method-verify + revise did NOT run
- `cumulative_incidence` — design only; verify did NOT run
- `standardization` — design only; verify did NOT run
- `calendar_trend` — design only; verify did NOT run
- `statistical_engine` — design only (agent errored after writing); verify did NOT run

**How these are used:** integration treats each file as a design reference only.
Every fixture number is re-derived by hand during integration, and the real gate
is the PGlite harness (executes the emitted SQL vs hand-computed ground truth)
plus the SAS↔SQL parity check. A blueprint is never trusted on its own arithmetic.
