{
  "name": "heor-studio",
  "private": true,
  "version": "0.0.0",
  "description": "HEOR Studio \u2014 protocol/SAP to verified, deterministic SAS + SQL for Merative MarketScan studies. Independent, open source; not affiliated with Merative.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @heor-studio/web",
    "build": "npm run build -w @heor-studio/web",
    "typecheck": "tsc -p packages/core/tsconfig.json && tsc -p packages/web/tsconfig.app.json && tsc -p packages/mcp/tsconfig.json",
    "verify": "npm run verify -w @heor-studio/core",
    "verify:snapshot": "tsx packages/core/src/verify/snapshot.ts check",
    "mcp:smoke": "npm run smoke -w @heor-studio/mcp",
    "lint": "oxlint",
    "sas:fixture": "npm run sas:fixture -w @heor-studio/core"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "oxlint": "^1.71.0",
    "tsx": "^4.23.1",
    "typescript": "~6.0.2"
  }
}
